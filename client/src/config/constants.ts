export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5002';

export const MAX_VIDEO_SIZE = 500 * 1024 * 1024; 
export const ALLOWED_VIDEO_TYPES = ['.mp4', '.mov', '.avi', '.webm'];

export const DEFAULT_USER_BALANCE = 500;

export const super_chat = [50, 100, 200, 500];

export const VIDEOS_PER_PAGE = 5;